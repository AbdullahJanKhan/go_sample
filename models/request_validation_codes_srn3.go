package models

// response code for invalid input body
const (
	INVALID_EMAIL_FORMAT = 3000
)

// response messages for invalid input body
const (
	INVALID_EMAIL_FORMAT_MSG = "Invalid Email Format"
)
