package models

// header keys
const (
	KeyCurrentUUID = "currentUUID"
	RequestKey     = "request"
)

// invalid request codes
const (
	INVALID_REQUEST_BODY = 2000
)

// invalid request message
const (
	INVALID_REQUEST_BODY_MSG = "Invalid Request Body: %v"
)
